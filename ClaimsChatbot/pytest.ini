[pytest]
testpaths = src/modules/test
asyncio_mode = auto
markers =
    integration: requires a live Neo4j connection (real dev data, read-only)
    live_llm: hits the real Ollama model -- slow (1-3+ min), non-deterministic
